# Server Port
server.port=8082

# Application Name (will register with this name in Eureka)
spring.application.name=account-service

# Eureka Server Registration URL
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
